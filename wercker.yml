box: wercker/nodejs
build:
  steps:
    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) runnin:g"
deploy:
  steps:
    - script:
        name: deploy to memelon.meteor.com 
        code: |
          curl https://install.meteor.com/ | sh
          meteor deploy memelon.meteor.com
